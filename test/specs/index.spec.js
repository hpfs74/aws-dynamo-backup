describe('Lambda handler', () => {
	describe('index', () => {
		it('should collect all available tables');
		it('should create chain of promises to run backup');
		it('should call callback with no error if success');
		it('should call callback with error if any');
	});
});
